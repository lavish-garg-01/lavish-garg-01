# Field Intelligence

The single V2 authority for turning value-free browser field evidence into a canonical semantic resolution and a value-free Candidate Truth answer reference.

It never reads the DOM, stores candidate answers, renders representation values, or executes controls.
